{
  "name": "emma-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist-main/main/main.js",
  "scripts": {
    "build:host": "cargo build --locked -p emma-host && zig build --build-file ../agent/build.zig",
    "build:renderer": "vite build",
    "build:main": "tsc -p tsconfig.main.json",
    "build:hotkey": "mkdir -p dist-native && clang -O2 -mmacosx-version-min=12.0 -fobjc-arc -framework AppKit -framework ApplicationServices native/quick_ask.m -o dist-native/emma-option-tap && dist-native/emma-option-tap --self-test",
    "build": "npm run build:main && npm run build:renderer",
    "dev": "node scripts/dev.mjs",
    "start": "npm run build:host && npm run build:hotkey && npm run build && electron .",
    "test": "npm run build:main && node --test dist-main/test/*.test.js",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "lint": "eslint . --max-warnings 0",
    "check": "npm run test && npm run typecheck && npm run lint && npm run build:renderer",
    "package:mac": "cargo build --locked --release -p emma-host && zig build -Doptimize=ReleaseSafe --build-file ../agent/build.zig && npm run build:hotkey && npm run build && electron-packager . Emma --platform=darwin --arch=arm64 --out=release --overwrite --asar --extra-resource=../target/release/emma-host --extra-resource=../agent/zig-out/bin/emma-agent --extra-resource=dist-native/emma-option-tap --app-bundle-id=dev.local.emma"
  },
  "devDependencies": {
    "@electron/packager": "20.0.4",
    "@eslint/js": "10.0.1",
    "@tailwindcss/vite": "4.3.3",
    "@types/node": "24.10.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "6.1.0",
    "electron": "43.4.0",
    "eslint": "10.0.1",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.4",
    "globals": "17.11.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vite": "8.2.2"
  },
  "dependencies": {
    "recharts": "3.10.1"
  }
}
